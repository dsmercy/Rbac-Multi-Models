global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

scrape_configs:
  - job_name: rbac-api
    static_configs:
      - targets: ["api:8080"]
    metrics_path: /metrics
    scheme: http
