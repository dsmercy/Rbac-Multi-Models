# Copy to .env.local for local development
# Never commit .env.local — it is in .gitignore

# Backend API base URL — all RTK Query calls are prefixed with this
VITE_API_BASE_URL=https://localhost:51090/api/v1

# SignalR hub URL
VITE_SIGNALR_HUB_URL=https://localhost:51090/api/v1/hubs/rbac
